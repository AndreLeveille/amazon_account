<h1>Edit a wish list</h1>


<%= form_with(model: @wish_list, local: true) do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, required: true, placeholder: "Name" %>
    <%= f.submit %>
  </div>
  
<% end %>
